services: 
  - type: web 
    name: my-ai-assistant 
    env: python 
    buildCommand: pip install -r requirements.txt 
    startCommand: gunicorn "app:app" 
    envVars: 
      - key: SECRET_KEY 
        generateValue: true 
